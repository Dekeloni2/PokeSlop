using FinalProject;

namespace TiltanTale.DesktopGL
{
    // Game1 with the desktop's answers filled in.
    //
    // There aren't any overrides here, and that's the point: Game1's
    // defaults were written for a window that can go fullscreen and close
    // itself, which is exactly what this is. The class exists so the desktop
    // has the same seam the browser build uses (see BrowserTiltanTaleGame),
    // rather than one platform reaching for Game1 directly and the other
    // being special.
    public class DesktopTiltanTaleGame : Game1
    {
    }
}
